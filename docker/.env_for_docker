#Possible values: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=${LOG_LEVEL}

#Possible values: FILE, STDOUT
LOG_TYPE=${LOG_TYPE}

#Only if LOG_TYPE=FILE is used
LOG_DIR=${LOG_DIR}
LOG_FILE=${LOG_FILE}
LOG_MAX_BYTES=${LOG_MAX_BYTES}
LOG_BACKUP_COUNT=${LOG_BACKUP_COUNT}

#EMAIL RELATED SETTINGS
MAIL_SERVER=${MAIL_SERVER}
MAIL_PORT=${MAIL_PORT}
MAIL_USE_TLS=${MAIL_USE_TLS}
MAIL_USERNAME=${MAIL_USERNAME}
MAIL_PASSWORD=${MAIL_PASSWORD}
MAIL_DEFAULT_SENDER=${MAIL_DEFAULT_SENDER}

ENABLE_MEDICINE_CHECKER=${ENABLE_MEDICINE_CHECKER}
MEDICINE_CHECK_INTERVAL=${MEDICINE_CHECK_INTERVAL}
SECRET_KEY=${SECRET_KEY}
MONGODB_URI=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}/${MONGO_DB}
STOCK_WARNING_TRESHOLD=${STOCK_WARNING_TRESHOLD}
